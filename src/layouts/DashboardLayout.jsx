import { Outlet } from "react-router-dom";

const DashboardLayout = () => {
    return (
        <>
            {/* Navbar */}
            {/* Sidebar */}

            <Outlet />
        </>
    );
};

export default DashboardLayout;